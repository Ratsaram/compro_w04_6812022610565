#include <stdio.h>

const int GLOBAL_RATE = 20;
void calculate() {
    const int LOCAL_RATE = 80;
    printf("Global Rate: %d\n", GLOBAL_RATE);
    printf("Local Bonus: %d\n", LOCAL_RATE);

}
int main() {
    calculate();
    printf("Global Rate in main: %d\n", GLOBAL_RATE);
    return 0;
}

//ค่าคงที่ Global Rate และ Local Bonus ต่างกันอย่างไร
  // ค่าคงที่ Global Rate เป็นค่าคงที่ที่ประกาศในระดับ global ซึ่งสามารถเข้าถึงได้จากทุกฟังก์ชันในโปรแกรม
  // ในขณะที่ Local Bonus  เป็นค่าคงที่ืี่ประกาศภายในฟังก์ชัน calculate() ซึ่งสามารถเข้าถึงได้เฉพาะภายในฟังก์ชันนั้นเท่านั้น
//เกิดอะไรขึ้นถ้าเราเขียน Global Rate = 20 และ Local Bonus = 80
  // เมื่อรันค่า จะมีการเปลี่ยนแปลงค่า Global Rate in main จาก 10 เป็น 20
//ทำไม Local Bonus ถึงไม่สามารถเข้าถึงได้ใน main
  // เพราะ Local Bonus ถูกประกาศภายในฟังก์ชัน calculate() ทำให้มันมีขอบเขตการใช้งานเฉพาะภายในฟังก์ชันเท่านั้น
//อภิปราย และสรุปผลจากการทดลอง
  // การใช้ค่าคงที่ Global Rate และ Local Bonus แสดงให้เห็นถึงความแตกต่างระหว่างค่าคงที่ที่ประกาศในระดับ global และ local
  // การประกาศค่าคงที่ในระดับ global ทำให้สามารถเข้าถึงได้จากทุกฟังก์ชันในโปรแกรม ในขณะที่ค่าคงที่ที่ที่ประกาศในระดับ local จะมีขอบเขตการใช้งานเฉพาะภายในฟังก์ชันนั้น
  // การเลือกใช้ค่าคงที่ที่เหมาะสมจะช่วยให้โปรแกรมทำงานได้อย่างมีประสิทธิภาพ และสามารถจัดการกับข้อมูลได้อย่างถูกต้อง